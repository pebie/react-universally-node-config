/**
 * FallBack
 * This component is used to resolve alias when no config entry is set by user.
 * It is a work around to avoid `RenderingEngineConfig` import fail.
 */
const FallbackConfig = () => null;
export default FallbackConfig;
